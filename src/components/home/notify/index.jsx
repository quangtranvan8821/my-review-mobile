import { View,Text } from "react-native"

export default Notify = ({ navigation }) => {
    return (
        <View>
            <Text>
                hello world
            </Text>
        </View>
    )
}